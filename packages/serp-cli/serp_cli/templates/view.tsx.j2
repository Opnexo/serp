/**
 * {{ view_name }} view
 * 
 * Generated by serp-cli
 */

import React from 'react';
{% if has_permission %}
import { usePermission } from '@serp/ui-shell/hooks/usePermission';
{% endif %}

export const {{ view_name }}: React.FC = () => {
  {% if has_permission %}
  const hasPermission = usePermission('{{ permission }}');

  if (!hasPermission) {
    return <div>Access Denied</div>;
  }
  {% endif %}

  return (
    <div className="container mx-auto p-4">
      <h1 className="text-2xl font-bold mb-4">{{ view_name }}</h1>
      {/* Add your component content here */}
    </div>
  );
};

export default {{ view_name }};
